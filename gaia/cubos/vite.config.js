export default {
    build: {
        outDir: 'curriculum/build', // Ensure this is the correct output directory
        rollupOptions: {
            input: 'curriculum/src/index.jsx', // The main entry point of your React app
        },
    }
};
